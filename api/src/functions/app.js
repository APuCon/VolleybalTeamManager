const{app}=require('@azure/functions');const{TableClient}=require('@azure/data-tables');const crypto=require('crypto');const cs=()=>process.env.VOLLEYBALL_STORAGE_CONNECTION;const tc=n=>TableClient.fromConnectionString(cs(),n);async function ready(c){try{await c.createTable()}catch(e){if(e.statusCode!==409)throw e}}function who(req){let h=req.headers.get('x-ms-client-principal');if(!h)return null;try{return JSON.parse(Buffer.from(h,'base64').toString('utf8'))}catch{return null}}function id(){return crypto.randomUUID()}function clean(v,n=150){return String(v||'').trim().slice(0,n)}function json(status,body){return{status,jsonBody:body}}async function member(teamId,userId){let c=tc('VTMTeamMembers');await ready(c);try{return await c.getEntity(teamId,userId)}catch(e){return null}}async function requireMember(teamId,p,roles){let m=await member(teamId,p.userId);return m&&(!roles||roles.includes(m.role))?m:null}
app.http('teams',{methods:['GET','POST'],authLevel:'anonymous',route:'teams/{teamId?}',handler:async(req)=>{let p=who(req);if(!p)return json(401,{error:'Niet ingelogd'});let teams=tc('VTMTeams'),members=tc('VTMTeamMembers');await ready(teams);await ready(members);let teamId=req.params.teamId;if(req.method==='POST'){let b=await req.json(),tid=id(),now=new Date().toISOString();await teams.createEntity({partitionKey:'team',rowKey:tid,teamId:tid,teamName:clean(b.teamName),season:clean(b.season,30),createdBy:p.userId,createdAt:now,status:'Active'});await members.createEntity({partitionKey:tid,rowKey:p.userId,email:p.userDetails||'',role:'Owner',joinedAt:now});return json(201,{teamId:tid})}if(teamId){let m=await member(teamId,p.userId);if(!m)return json(403,{error:'Geen toegang'});let t=await teams.getEntity('team',teamId);return{jsonBody:{teamId,teamName:t.teamName,season:t.season,role:m.role}}}let arr=[];for await(const m of members.listEntities({queryOptions:{filter:`RowKey eq '${p.userId}'`}})){try{let t=await teams.getEntity('team',m.partitionKey);arr.push({teamId:t.teamId,teamName:t.teamName,season:t.season,role:m.role})}catch{}}return{jsonBody:arr}}});
app.http('players',{methods:['GET','POST'],authLevel:'anonymous',route:'players',handler:async(req)=>{let p=who(req),c=tc('VTMPlayers');if(!p)return json(401,{error:'Niet ingelogd'});await ready(c);if(req.method==='POST'){let b=await req.json(),m=await requireMember(b.teamId,p,['Owner','Coach']);if(!m)return json(403,{error:'Geen schrijfrechten'});let x=id();await c.createEntity({partitionKey:b.teamId,rowKey:x,playerId:x,name:clean(b.name),number:clean(b.number,10),role:clean(b.role,30),active:true});return json(201,{playerId:x})}let teamId=req.query.get('teamId');if(!await requireMember(teamId,p))return json(403,{error:'Geen toegang'});let a=[];for await(const x of c.listEntities({queryOptions:{filter:`PartitionKey eq '${teamId}'`}}))a.push(x);return{jsonBody:a}}});
app.http('matches',{methods:['GET','POST'],authLevel:'anonymous',route:'matches',handler:async(req)=>{let p=who(req),c=tc('VTMMatches');if(!p)return json(401,{error:'Niet ingelogd'});await ready(c);if(req.method==='POST'){let b=await req.json();if(!await requireMember(b.teamId,p,['Owner','Coach']))return json(403,{error:'Geen schrijfrechten'});let x=id();await c.createEntity({partitionKey:b.teamId,rowKey:x,matchId:x,opponent:clean(b.opponent),matchDate:clean(b.matchDate,20),location:clean(b.location),status:'Planned',createdAt:new Date().toISOString()});return json(201,{matchId:x})}let teamId=req.query.get('teamId');if(!await requireMember(teamId,p))return json(403,{error:'Geen toegang'});let a=[];for await(const x of c.listEntities({queryOptions:{filter:`PartitionKey eq '${teamId}'`}}))a.push(x);return{jsonBody:a}}});
app.http('members',{methods:['GET'],authLevel:'anonymous',route:'members',handler:async(req)=>{let p=who(req),teamId=req.query.get('teamId');if(!p||!await requireMember(teamId,p))return json(403,{error:'Geen toegang'});let c=tc('VTMTeamMembers');await ready(c);let a=[];for await(const x of c.listEntities({queryOptions:{filter:`PartitionKey eq '${teamId}'`}}))a.push(x);return{jsonBody:a}}});
app.http('invitations',{methods:['POST'],authLevel:'anonymous',route:'invitations',handler:async(req)=>{let p=who(req),b=await req.json();if(!p||!await requireMember(b.teamId,p,['Owner']))return json(403,{error:'Alleen eigenaar'});let c=tc('VTMInvitations');await ready(c);let email=clean(b.email).toLowerCase();await c.upsertEntity({partitionKey:b.teamId,rowKey:crypto.createHash('sha256').update(email).digest('hex'),email,role:['Coach','Viewer'].includes(b.role)?b.role:'Viewer',createdAt:new Date().toISOString(),createdBy:p.userId},'Replace');return json(201,{ok:true})}});
app.http('dashboard',{methods:['GET'],authLevel:'anonymous',route:'dashboard',handler:async(req)=>{let p=who(req),teamId=req.query.get('teamId');if(!p||!await requireMember(teamId,p))return json(403,{error:'Geen toegang'});async function count(name){let c=tc(name);await ready(c);let n=0;for await(const x of c.listEntities({queryOptions:{filter:`PartitionKey eq '${teamId}'`}}))n++;return n}return{jsonBody:{players:await count('VTMPlayers'),matches:await count('VTMMatches'),sets:await count('VTMSets')}}}});
app.http('report',{methods:['GET'],authLevel:'anonymous',route:'report',handler:async(req)=>{let p=who(req),teamId=req.query.get('teamId');if(!p||!await requireMember(teamId,p))return json(403,{error:'Geen toegang'});let result={teamId,generatedAt:new Date().toISOString(),players:[],matches:[],sets:[],attendance:[],substitutions:[]};for(const [k,n] of Object.entries({players:'VTMPlayers',matches:'VTMMatches',sets:'VTMSets',attendance:'VTMAttendance',substitutions:'VTMSubstitutions'})){let c=tc(n);await ready(c);for await(const x of c.listEntities({queryOptions:{filter:`PartitionKey eq '${teamId}'`}}))result[k].push(x)}return{jsonBody:result}}});
